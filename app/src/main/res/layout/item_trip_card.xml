<?xml version="1.0" encoding="utf-8"?>
<!--
  Author: Jinglin Lei
  Assignment: Trip Card
  Updated: remove top placeholder image, add dynamic chips container
-->

<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/field_gap"
    app:cardCornerRadius="@dimen/field_corner"
    app:strokeColor="@color/field_stroke"
    app:strokeWidth="2dp"
    app:cardBackgroundColor="@android:color/white"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- 去掉顶部灰色占位图 -->

        <!-- 信息区 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <TextView
                android:id="@+id/tv_title"
                style="@style/TextAppearance.EqualTrip.Label"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                tools:text="Unimelb Trip" />

            <TextView
                android:id="@+id/tv_city"
                style="@style/TextAppearance.EqualTrip.Terms"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/text_secondary"
                tools:text="Melbourne, VIC" />

            <TextView
                android:id="@+id/tv_meta"
                style="@style/TextAppearance.EqualTrip.Terms"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/text_secondary"
                tools:text="Aug 1–5, 2025 | Tripmates: 3" />

            <!-- 动态小圆点容器（由 Adapter 填充） -->
            <LinearLayout
                android:id="@+id/chips_container"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp"
                android:overScrollMode="never"
                tools:ignore="UseCompoundDrawables">
            </LinearLayout>

        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
