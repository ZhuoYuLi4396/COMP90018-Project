<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 基础网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ===== BLE / 蓝牙能力（建议：可选，避免限制可安装设备） ===== -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <!-- Android 12+ (API 31+) 新蓝牙权限（避免重复，只保留一份） -->
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- 兼容 Android 10 及以下 (API <=30) 的旧蓝牙权限（仅在旧系统生效） -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- BLE 扫描在 Android 10 及以下需要定位权限 -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />
    <!-- 如果想兼容更老机型，再补 COARSE 但同样限定到旧系统 -->
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="28" />

    <!-- 相机（可选，运行时动态申请） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 仅当真的要读图库且 targetSdk>=33 再开启 -->
    <!-- <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> -->

    <!-- Android 11+ queries：合并为一个区块即可 -->
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="image/*" />
        </intent>
    </queries>

    <!-- 相机能力可选，避免强依赖 -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EqualTrip">

        <!-- 唯一启动入口（有 intent-filter 就必须 exported=true，已设置） -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.EqualTrip.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 其他页面不带 intent-filter，可不写 exported（或显式 false/true 均可） -->
        <activity android:name=".SignInActivity" android:exported="true" />
        <activity android:name=".HomeActivity" android:exported="false" android:launchMode="singleTask"/>
        <activity android:name=".SignUpActivity" android:exported="false" />
        <activity android:name=".ForgotPasswordActivity" android:exported="false" android:theme="@style/Theme.EqualTrip" />

        <activity android:name=".TripPageActivity" android:exported="false" android:launchMode="singleTask"/>
        <activity android:name=".TripDetailActivity" />
        <activity android:name=".AddTripActivity" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".EditTripActivity" />

        <activity android:name=".AddBillActivity" android:exported="false" />
        <activity android:name=".PayerSelectionActivity" android:exported="false" android:parentActivityName=".AddBillActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".AddBillActivity" />
        </activity>
        <activity android:name=".ParticipantsSelectionActivity" android:exported="false" android:parentActivityName=".AddBillActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".AddBillActivity" />
        </activity>

        <activity android:name=".ProfileActivity" android:exported="false" android:launchMode="singleTask" />
        <activity android:name=".AlgorithmTestActivity" android:exported="true" />

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
